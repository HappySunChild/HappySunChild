# HappySunChild
a person

## Usage
you cant use this person because they are not available

```lua
local outside = true
local inside = false

local function goInside()
  outside = false
  inside = true
end

while outside do
  goInside()
end
```
